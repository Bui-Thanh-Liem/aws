# Create access key and secret key for user

# Configure AWS CLI with access key and secret key

# Test AWS CLI access with the user's credentials

# Create a policy mfa-policy requiring MFA for sensitive actions

# Test cmd with and without MFA

# Config multi-factor authentication (MFA) device for the user

# Test AWS CLI access with MFA-enabled credentials
