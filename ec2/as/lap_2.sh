# Create dynamic scaling 
    - Belong Metric type: CPU, network, request ...
    - Set threshold to scale out/in
    - Set min, max, desired capacity
    - Attach to existing load balancer

# Create scheduled scaling
    - Set time to scale out/in
    - Set min, max, desired capacity
    - Attach to existing load balancer

