# EC2 - (Elastic Compute Cloud)

# AMI - (Amazon Machine Images) (ISO)

# Lunch template - like file config, ami, vpc, Key pair, ...

# EBS - (Elastic Block Store) - Volume, Snapshot

# VPC - (Virtual Private Cloud) (LAN) - Subnets (public/private)

# Security groups (firewall)

# Key pairs

# IP - Public - Private - Elastic (tạo mà không dùng sẽ bị tính phí)

# CloudWatch

1. Create instance base

2. Create instance from ami

3. Create instance with scripts (user data)

# ===== COST => (instance) - (volume, snapshot) - (elastic ip) - (load balancer)

# On-Demand

    - Dùng bao nhiêu trả bấy nhiêu (đắt nhất)

# Reserved Instances (RI)

    - Thuê dài hạn 1-3 năm (giảm tới 70% giá)

# Spot Instances

    - Đấu giá máy thừa của AWS (rẻ nhất, nhưng máy có thể bị thu hồi bất cứ lúc nào)
