# Create 2 instances ec2

# Create 2 target group, register instances (each instance in separate target group)

# Create Application Load Balancer, attach target group to ALB 

# Create listener for ALB on port 80, fixed radio 25-75 between target groups